ntuple_path: "root://cmsdcache-kit-disk.gridka.de//store/user/sgiappic/CROWN/ntuples/htt_260602_2024-25_v5/CROWNRun/" #root://cmsdcache-kit-disk.gridka.de//store/user
output_path: "/ceph/sgiappic/CMS_FF/260602/"
#friends: 
file_path: "/ceph/sgiappic/CMS_FF/260602/"

workdir_name: htt_260602

era: '2025'
tree: ntuple
nanoAOD_version: "nanoAOD_v15"
sample_database: "/work/sgiappic/TauKITFlow/datasets"

tau_vs_jet_wps:
  - VVVLoose
  - Medium
tau_vs_jet_wgt_wps:
  - Medium

# Use the precomputed CROWN selection mask branches (presel_mask, ff_*) instead of
# re-applying the cut strings of the preselection/fake factor configs. Falls back to the
# cut strings automatically if the branches are not present on the input n-tuples.
# Set to false to force the legacy cut string selection (A/B validation).
use_region_masks: true

use_embedding: false
use_center_of_mass_bins: true

skip_corrections_compatible_to_one: true
skip_corrections_p_value: 0.05
use_suppressed_mc_errors_for_correction_selection: true